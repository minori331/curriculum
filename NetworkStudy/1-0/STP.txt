問１
ルートブリッジ・・・switch3
（ブリッジプライオリティが１番小さいため）

問２
switch1のF0/1・・・指定ポート/DP
（ルートポートの向かいのポートは必ず指定ポートになるため）
switch1のF0/2・・・ルートポート/RP
（ルートブリッジまでのルートパスコストがより小さいため）
switch2のF0/1・・・ルートポート/RP
（ルートブリッジまでのルートパスコストがより小さいため）
switch2のF0/2・・・ブロッキングポート/BP
（ルートポート、指定ポートどちらにも指定されないポートのため）
switch3のE0/1・・・指定ポート/DP
（ルートブリッジの全てのポートは必ず指定ポートになるため）
switch3のF0/2・・・指定ポート/DP
（ルートブリッジの全てのポートは必ず指定ポートになるため）
switch4のE0/1・・・指定ポート/DP
（送信元スイッチのブリッジIDが小さい方を指定ポートとするため）
switch4のE0/2・・・ルートポート/RP
（ルートブリッジまでのルートパスコストがより小さいため）